services:
  rabbitmq:
    image: "rabbitmq:alpine"
    ports:
      - "5672:5672"
  prmq-build:
    depends_on:
      - rabbitmq
    build: .
    volumes:
      - ../:/PRMQ
    extra_hosts:
      - "testhost:172.16.3.224"
